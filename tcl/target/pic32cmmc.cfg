# SPDX-License-Identifier: GPL-2.0-or-later

#
# Microchip PIC32CM-MC target
#
# Notes:
#   - SWD transport only (no JTAG).
#   - PIC32CM-MC uses the legacy plain NVMCTRL reset path with no BCC-mailbox
#     handshake (has_bootrom_dal=false).  No DSU boot-ROM extension is needed.
#   - PIC32CM-MC has a real DSU_DID and is auto-probed normally.  Setting
#     PIC32CMMC_VARIANT is optional; it forces a specific part name and
#     skips the DID probe (useful if DID auto-detect ever misidentifies).
#

transport select swd

# Optional part variant name override -- see pic32cmmc_parts[] in
# src/flash/nor/at91samd.c for valid names.
if { [info exists PIC32CMMC_VARIANT] } {
    set _VARIANT $PIC32CMMC_VARIANT
} else {
    set _VARIANT ""
}

if { [info exists CHIPNAME] } {
    set _CHIPNAME $CHIPNAME
} else {
    set _CHIPNAME pic32cmmc
}

if { [info exists WORKAREASIZE] } {
   set _WORKAREASIZE $WORKAREASIZE
} else {
   set _WORKAREASIZE 0x800
}

# -expected-id omitted: not confirmed against hardware.
swd newdap $_CHIPNAME cpu
dap create $_CHIPNAME.dap -chain-position $_CHIPNAME.cpu

set _TARGETNAME $_CHIPNAME.cpu
target create $_TARGETNAME cortex_m -dap $_CHIPNAME.dap

$_TARGETNAME configure -work-area-phys 0x20000000 -work-area-size $_WORKAREASIZE -work-area-backup 0

set _FLASHNAME $_CHIPNAME.flash
if { $_VARIANT ne "" } {
    flash bank $_FLASHNAME at91samd 0 0 0 0 $_TARGETNAME $_VARIANT
} else {
    flash bank $_FLASHNAME at91samd 0 0 0 0 $_TARGETNAME
}

$_TARGETNAME configure -event examine-end {
    dsu_reset_deassert
}

$_TARGETNAME configure -event reset-deassert-post {
    dsu_reset_deassert
}

if {![using_hla]} {
    cortex_m reset_config sysresetreq
}
